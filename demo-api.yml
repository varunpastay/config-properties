server:
  port: 8082

spring:
  boot:
    admin:
      client:
        url: http://localhost:1111

management:
  endpoints:
    web:
      exposure:
        include: "*"

message: "this message is from config server"
