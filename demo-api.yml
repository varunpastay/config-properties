server:
  port: 8082

spring:
  boot:
    admin:
      client:
        url: http://localhost:1111

management:
  endpoints:
    web:
      expoure:
      include: "*"

message:"this msh is from config server"
